# ハンドテスト手順書

全機能のテスト手順（2026-02-27 更新: セッション維持修正・AWS版対応）

## 前提条件

- **ローカル版**: バックエンド (`uvicorn app.main:app`) + フロントエンド (`npm run dev`) 起動済み
- **AWS版**: https://main.d1eqc0nk6zbqop.amplifyapp.com (認証: admin / password123)
- **EC2直接版**: http://54.249.148.244
- LLMプロバイダー設定済み (OpenAI APIキー)
- 車両マニュアルがRAGに登録済み（フィット）

---

## テスト0: セッション維持（AWS版必須）

### 目的
複数リクエストにまたがってセッションが切れないことを確認する。

### 手順
1. AWS版にアクセス
2. 車両を選択（例: フィット）
3. 写真確認で「はい」
4. 症状を入力
5. 問診に **3回以上** 回答する

### 期待結果
- [ ] 途中で「セッションの有効期限が切れました」が **表示されない**
- [ ] 全ステップが正常に遷移する（vehicle_id → photo_confirm → free_text → diagnosing）
- [ ] 問診が最後まで完了できる

---

## テスト1: 警告灯アイコンカード表示

### 目的
警告灯に関する質問で画像カードが表示されることを確認する。

### 手順
1. チャットを開始
2. 車両を選択（例: フィット）
3. 症状入力: 「ダッシュボードの警告灯が点灯している」
4. LLMが「どの警告灯が点灯していますか？」のような質問を生成するまで待機

### 期待結果
- [ ] 選択肢がテキストボタンではなく **3列のアイコンカード** で表示される
- [ ] 各カードにSVGアイコン（エンジン、ABS、バッテリー等）が表示される
- [ ] カードの下に「わからない」「✏️ 自由入力」がテキストボタンとして配置される
- [ ] アイコンカードをタップすると正常に選択され、問診が続行する
- [ ] 「わからない」を選択しても正常に続行する

---

## テスト2: 警告灯以外の質問ではテキストボタン表示

### 目的
警告灯以外の質問では従来通りテキストボタンが表示されることを確認する。

### 手順
1. チャットを開始
2. 車両を選択
3. 症状入力: 「走行中に異音がする」
4. 問診の質問を確認

### 期待結果
- [ ] 選択肢がアイコンカードではなく **従来通りのテキストボタン** で表示される
- [ ] 「わからない」「✏️ 自由入力」も同じ行に配置される

---

## テスト3: マニュアルに載っている症状 → `manual_coverage = covered`

### 目的
マニュアル記載済みの症状で正常な表示を確認する。

### 手順
1. チャットを開始
2. マニュアル登録済みの車両を選択（例: フィット）
3. 症状入力: 「セレクトレバーが動かない」
4. 問診に回答（2〜3ターン）
5. provide_answer が表示されるまで進める

### 期待結果
- [ ] 回答メッセージに「ディーラーでの点検を推奨」の警告が **ない**
- [ ] 黄色バッジ「マニュアル記載外」が **表示されない**
- [ ] マニュアル参照ページが表示される

---

## テスト4: マニュアルに載っていない症状 → `manual_coverage = not_covered`

### 目的
マニュアル記載外の症状で警告が正しく表示されることを確認する。

### 手順
1. チャットを開始
2. 車両を選択
3. 症状入力: 「走行中にハンドルが左に引っ張られる」（マニュアルに通常記載がない症状）
4. 問診に回答
5. provide_answer まで進める

### 期待結果
- [ ] 回答メッセージ末尾に「⚠️ マニュアルに記載のない症状のため、ディーラーでの点検を推奨します」が表示される
- [ ] 黄色バッジ「⚠ マニュアル記載外 — 想定外の不具合の可能性があります」が表示される
- [ ] urgency が none/low だった場合、medium に引き上げられている

---

## テスト5: ブレーキ故障 → CRITICAL即時判定

### 目的
CRITICALキーワードで即座に緊急フローに遷移することを確認する。

### 手順
1. チャットを開始
2. 車両を選択
3. 症状入力: 「ブレーキが効かない」

### 期待結果
- [ ] キーワード判定で即座に CRITICAL 判定（問診をスキップ）
- [ ] 予約画面（RESERVATION）に遷移
- [ ] UrgencyAlert に赤太字「🚨 運転を中止してください」が表示される
- [ ] `visit_urgency = immediate` のラベルが表示される
- [ ] 選択肢に「ロードサービスを呼ぶ」が表示される
- [ ] 「ディーラーに持ち込む」は **表示されない**（can_drive = false のため）

---

## テスト6: 警告灯点灯 → HIGH判定（can_drive = true）

### 目的
運転可能だが早急な点検が必要なケースの表示を確認する。

### 手順
1. チャットを開始
2. 車両を選択
3. 症状入力: 「エンジン警告灯が点灯している」
4. 問診に回答（必要に応じて）

### 期待結果
- [ ] urgency_level が high
- [ ] UrgencyAlert に「本日中の来場をおすすめします」ラベルが表示される
- [ ] 「🚨 運転を中止してください」は **表示されない**（can_drive = true）
- [ ] 来店予約とロードサービスの両方の選択肢が表示される

---

## テスト7: 問診3ターン後に「やり直す」

### 目的
やり直し（Rewind）機能の動作を確認する。

### 手順
1. チャットを開始
2. 車両を選択
3. 症状入力: 「エンジンから異音がする」
4. 問診1回目: 適当に回答（例: 「走行中に聞こえる」）
5. 問診2回目: 適当に回答（例: 「カタカタという音」）
6. 問診3回目: 適当に回答（例: 「最近始まった」）
7. **問診1回目のユーザーメッセージ**の下に「やり直す」リンクが表示されていることを確認
8. 「やり直す」をクリック

### 期待結果
- [ ] DIAGNOSING 中のユーザーメッセージ（最新以外）に「やり直す」リンクが表示される
- [ ] 最新のユーザーメッセージには「やり直す」が **表示されない**
- [ ] 「やり直す」クリック後、そのメッセージ以降のやり取りがチャットから削除される
- [ ] バックエンドのセッション状態がそのターンの時点に復元される
- [ ] その後、別の回答を入力して問診を続行できる

---

## テスト8: やり直し後に完了まで進む

### 目的
やり直し後の問診が正常に完了することを確認する。

### 手順
1. テスト7の状態から続行
2. やり直し後、別の回答を入力
3. provide_answer まで進める
4. 「解決しました」を選択

### 期待結果
- [ ] provide_answer が正常に表示される
- [ ] 「解決しました」選択で DONE ステップに遷移
- [ ] 完了メッセージが表示される

---

## テスト9: 仕様確認フロー（Spec Check）

### 目的
症状がマニュアル上の正常動作である場合の仕様確認フローを確認する。

### 手順
1. チャットを開始
2. マニュアル登録済みの車両を選択
3. 症状入力: 「アイドリングストップが作動しない」（マニュアルに仕様記載がある症状）

### 期待結果
- [ ] SPEC_CHECK ステップに遷移する
- [ ] 「マニュアルを確認したところ、これは仕様（正常な動作）の可能性があります」のメッセージが表示される
- [ ] 選択肢に「解決しました」「解決していません」「それは試しました」が表示される
- [ ] 「解決しました」→ 完了
- [ ] 「解決していません」→ DIAGNOSING に遷移して問診を継続

---

## テスト10: 複合確認（not_covered + visit_urgency + 警告灯アイコン）

### 目的
複数の機能が同時に正しく動作することを確認する。

### 手順
1. チャットを開始
2. 車両を選択
3. 症状入力: 「見たことのない警告灯が点灯して、ステアリングが重くなった」
4. 警告灯の質問が出たら、アイコンカードから選択
5. 問診を続行

### 期待結果
- [ ] 警告灯質問時にアイコンカードが表示される
- [ ] アイコンカードから選択して問診が続行する
- [ ] manual_coverage が not_covered または partially_covered
- [ ] visit_urgency が設定されている（today または this_week）
- [ ] 両方の表示（バッジ + visit_urgencyラベル）が同時に正しく表示される

---

## テスト11: 「自由入力」ボタンの動作

### 目的
選択肢の「自由入力」ボタンが正常に動作することを確認する。

### 手順
1. テスト1と同様に警告灯アイコンカードを表示させる
2. カード下部の「✏️ 自由入力」ボタンをクリック

### 期待結果
- [ ] テキスト入力欄にフォーカスが移動する
- [ ] メッセージは送信されない
- [ ] 自由にテキストを入力して送信できる

---

## テスト12: AWS版 — Amplify + API Gateway + EC2 結合

### 目的
Amplify → API Gateway → EC2 の結合が正しく動作することを確認する。

### 手順
1. https://main.d1eqc0nk6zbqop.amplifyapp.com にアクセス
2. Basic認証: admin / password123
3. テスト0〜8 の手順を実行

### 期待結果
- [ ] 車両検索API (`/api/vehicles/search`) が正常にレスポンスを返す
- [ ] チャットAPI (`/api/chat`) が正常にレスポンスを返す
- [ ] セッションが途中で切れない
- [ ] ローカル版と同等の機能が動作する

---

## テスト13: EC2直接版 — Nginx + FastAPI

### 目的
EC2のNginx経由でフロントエンド + バックエンドが正常に動作することを確認する。

### 手順
1. http://54.249.148.244 にアクセス
2. テスト0〜8 の手順を実行

### 期待結果
- [ ] フロントエンドが正常に表示される
- [ ] APIがNginxリバースプロキシ経由で正常に動作する
- [ ] セッションが途中で切れない

---

## 確認用チェックリスト（開発者向け）

### バックエンド確認コマンド
```bash
# 全ファイルのコンパイルチェック
python3 -m py_compile backend/app/models/session.py
python3 -m py_compile backend/app/models/chat.py
python3 -m py_compile backend/app/llm/schemas.py
python3 -m py_compile backend/app/llm/prompts.py
python3 -m py_compile backend/app/chat_flow/step_diagnosing.py
python3 -m py_compile backend/app/services/urgency_assessor.py
python3 -m py_compile backend/app/chat_flow/step_urgency.py
python3 -m py_compile backend/app/chat_flow/step_reservation.py
```

### フロントエンド確認コマンド
```bash
cd frontend && npm run build
```

### AWS版ヘルスチェック
```bash
# API Gateway経由
curl -s https://m575kctmch.execute-api.ap-northeast-1.amazonaws.com/api/health

# EC2直接
curl -s http://54.249.148.244/api/health
```

### スキーマ確認
- `DIAGNOSTIC_SCHEMA` に `manual_coverage`, `visit_urgency` が存在すること
- `URGENCY_SCHEMA` に `visit_urgency` が存在すること
- `SessionState` に `state_snapshots`, `visit_urgency`, `manual_coverage` が存在すること

### 警告灯アイコン確認
- `frontend/public/icons/warning-lights/` に10個のSVGファイルが存在すること
- `step_diagnosing.py` の `WARNING_LIGHT_ICONS` 辞書に13キーワード→10アイコンのマッピングがあること
- `ChoiceButtons.tsx` に `hasIcons` による分岐ロジックがあること

### インフラ確認
- EC2: `--workers 1` で起動していること（インメモリセッションのため必須）
- Amplify リライトルール: `/api/<*>` → API Gateway
- API Gateway: `m575kctmch` → EC2:8000
